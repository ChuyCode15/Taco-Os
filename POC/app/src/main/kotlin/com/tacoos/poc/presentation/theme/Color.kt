package com.tacoos.poc.presentation.theme

import androidx.compose.ui.graphics.Color

/**
 * Paleta de colores profesional y confiable (Estilo iOS).
 */

/** Azul marino primario para confianza y estabilidad. */
val PrimaryNavy = Color(0xFF1E293B)
/** Azul de acción clásico de iOS. */
val ActionBlue = Color(0xFF007AFF)
/** Verde para indicar éxito o crecimiento. */
val SuccessGreen = Color(0xFF34C759)
/** Rojo para errores y alertas. */
val ErrorRed = Color(0xFFFF3B30)
/** Ámbar para advertencias. */
val WarningAmber = Color(0xFFFFCC00)

/** Fondo blanco del sistema estilo iOS. */
val BackgroundWhite = Color(0xFFF2F2F7)
/** Color de superficie para tarjetas. */
val SurfaceCard = Color(0xFFFFFFFF)
/** Texto principal en negro. */
val TextPrimary = Color(0xFF000000)
/** Texto secundario en gris. */
val TextSecondary = Color(0xFF8E8E93)
/** Color para divisores. */
val DividerGray = Color(0xFFC6C6C8)

/** Superposición azul translúcida (Glassmorphism). */
val GlassBlue = Color(0xCC1E293B) 
/** Superposición blanca translúcida (Glassmorphism). */
val GlassWhite = Color(0xCCFFFFFF)

/** Colores para el Modo Oscuro estilo Apple. */

/** Gris oscuro elegante para el fondo. */
val DarkBackground = Color(0xFF1B1B1B)
/** Superficie oscura estilo Apple. */
val DarkSurface = Color(0xFF2C2C2E)
/** Azul brillante para acciones en modo oscuro. */
val DarkActionBlue = Color(0xFF0A84FF)
/** Texto principal blanco. */
val DarkTextPrimary = Color(0xFFFFFFFF)
/** Texto secundario claro. */
val DarkTextSecondary = Color(0xFFEBEBF5)
/** Divisor oscuro. */
val DarkDivider = Color(0xFF38383A)
